# Kibana Notes

Recommended data views:

- `earthquake-events-*` with `event_time_utc` as the time field
- `earthquake-daily-summary-v1` with `event_date` as the time field

Dashboard panels are described in [`docs/dashboard-spec.md`](../docs/dashboard-spec.md).

